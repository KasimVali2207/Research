defaults:
  - base
  - _self_

experiment:
  name: agentic_system
  description: "Full 5-agent LLM orchestration pipeline"
  cancer_types: [colorectal, lung, liver]
  horizons: [3, 6, 12]
  models: [xgboost]
  use_temporal: true
  use_agents: true
  use_rag: true
