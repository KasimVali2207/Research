defaults:
  - base
  - _self_

experiment:
  name: external_validation
  description: "Train on MIMIC-IV, test on eICU"
  train_dataset: mimic
  test_dataset: eicu
  cancer_types: [colorectal, lung, liver]
  horizons: [3, 6, 12]
  models: [xgboost, lightgbm]
  use_temporal: true
