defaults:
  - base
  - _self_

experiment:
  name: temporal_vs_static
  description: "Compare temporal trajectory features vs static snapshot"
  cancer_types: [colorectal, lung, liver]
  horizons: [3, 6, 12]
  models: [xgboost, lightgbm]
  use_temporal: true
  use_agents: false
