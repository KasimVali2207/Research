defaults:
  - base
  - _self_

experiment:
  name: baseline_ml
  description: "All baseline ML models, static features, all cancer types"
  cancer_types: [colorectal, lung, liver]
  horizons: [3, 6, 12]
  models: [logistic_regression, random_forest, xgboost, lightgbm, catboost, tabnet]
  use_temporal: false
  use_agents: false
